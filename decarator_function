#A decorator in Python is a special function that allows 
you to modify or extend the behavior of another function without changing its actual code.
def add_sprinkles(func):
    def wrapper(*args,**kwargs):
      print("adding sprinkles")
      func(*args,**kwargs)
    return wrapper
@add_sprinkles
def get_ice_cream(flavor,size):
    print(f" here is your {size} {flavor} ice cream")
get_ice_cream(vanilla)
